1)Write a JavaScript program to merge both objects by summing the values of matching keys.let a1 = { a: 1, b: 2 }; let a2 = { a: 4, b: 6, c: 3 }; { a: 5, b: 8, c: 3 }
1)Write a JavaScript program to rotate an array to the left by 2 positions without using any inbuilt array methods.
Input: [9, 6, 4, 3, 2] Output: [4, 3, 2, 9, 6]
1)find duplicate elements from given array.
2)find the common elements between two given arrays.
3)How to find the max and min number from given array.
4)find the second maximum element from given array(Hint:: duplicates remove, sort, find second last).
5)find the smallest missing Number from the given array, i / p: array [3, 5, 4, 7,9, 10].    o/p: 6
6)find the Maximum repeated element from given array [1, 2, 2, 3, 3, 3, 4, 4, 4, 1, 5, 5, 5, 5, 5];
7)Generate a 4 digit Random OTP number.
8)find the commen elements between two objects.input1 = {a: 1, b: 2, c: 3,d:4);input2 = { d: 4, e: 5 }; output: { d: 4}
9)input= { name:"ramu", age:30,gender:"male"},output=[[name, ramu], [age, 30], [gender, male]];
10)Find fibanoci series of length 10.
11)create a counter to find the number of days before 2025 Jan 1st. (Ans should in form of 100Days 5Hrs 20Min 3 sec).
12)Implement PolyFill for Debouncing and tHrottling?
13)Explain about Event propagation and Event Delegation?
14)create a model which needs to close on click of negative space?
15))Give one best example for arrow function over normal functin and normal function over arrow function.
16)Given an array of numbers, write a JavaScript program to categorize the elements into three groups: numbers between 1 and 99, numbers between 100 and 199, and numbers greater than or equal to 200, and store the result in a nested array. i/p:let array = [100, 1, 200, 2, 4, 5, 102, 201, 105]; o/p: [ [ 1, 2, 4, 5 ], [ 100, 102, 105 ], [ 200, 201 ] ] list into flat list.
================================= HIGH level DSA =========================
1)Find the longest Substring Without Repeating Characters from a given String;
2)Given a binary array numbers, return the maximum number of consecutive 1’s in the array.
3)Merge 2 given arrays and sort them and dont use new array, use existing array. nums1 = [3, 1, 7];  nums2 = [4, 2, 6];
4)Given an array nums containing n distinct numbers in the range [, n], return the only number in the range that is missing from the array.
5)find all missing numbers in the give range of numbers in the array.
6)Given an integer array nums, move all 0’s to the end of array/ start of array(Both may ask) while maintaining the relative order of the non-zero elements.Note:You must do this in-place without making a copy of the array.
7)You are given an unsorted array that contains duplicate elements.Write a JavaScript function to remove the duplicate values and return a new array containing only unique elements.
8)Remove all occurrences of particular val from nums array in-place and return the remaining elements.(Don't create new array).
9)Given a non-empty array of integers nums, every element appears twice except for one. Find that single one.
10)Best Time to buy and sell stocks problem(find maxProfit,BestBuying Day and Best Selling Day)
11)Given an integer array nums of unique elements, return all possible subsets (the power set 2^n).The solution set must not contain duplicate subsets. Return the solution in any order.Input: [1,2,3]  ----->>>>>  Output: [[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]].
12)Rabbit Population Growth Problem (a classic in DSA and recursion). it is same as Fibanocii series  , check in CGPT.
13)Given a sorted array and a target value, return the index if found. If not, return the index where it should be inserted.(Use Binary search).
14)Given a strictly increasing sorted array of positive integers arr and an integer k, return the kth missing positive integer that does not appear in the array.Input: arr = [2,3,4,7,11], k = 5 ----->>>>> Output: 9 Explanation: The missing positive integers are [1,5,6,8,9,10,12,13,...]. The 5th missing positive integer is 9.
15)Count of Smaller Numbers After Self Given an array nums, for each element, count how many numbers after it are smaller. nums = [5, 2, 6, 1]; // Output: [2,1,1,0]
16)Sort Colors (Dutch National Flag(DNF) Problem) with any three types of elements and length is infinity.
17)Given an array of integers, find the first and last position of a given target. If not found, return [-1, -1].
18)Find Second Largest Element from the given array having duplicte elements(without sorting)Input: [10, 5, 20, 20, 8, 12].
19)Find the first Repeating and first non repeating element from the given array  arr = [4, 5, 1, 2, 1, 5, 3];
20)find the pivot of given array,The index where the array was rotated in a rotated sorted array.(A rotated sorted array is one where an originally sorted array has been shifted.)
Ex: arr = [4,5,6,7,0,1,2],Here the pivot is at index 4 (0), because that’s where the smallest element is, and rotation happened.
21)Write a JavaScript function that performs run-length encoding on a given string by compressing consecutive repeating characters into the character followed by its count. Input: "abbbcccadeff" Output: "a1b3c3a1d1e1f2".
22)Given an array of strings, group the strings that are anagrams of each other and return the grouped result.Input:["eat", "tea", "tan", "ate", "nat", "bat"] Output:[["eat","tea","ate"], ["tan","nat"], ["bat"]].
23)Given two strings s and t, determine whether they are anagrams of each other. s = "listen",t = "silent".
24)Given an integer array arr, find the maximum possible sum of a contiguous subarray using Kadane’s Algorithm.(Part A: Maximum Sum Only,Write a function that returns the maximum subarray sum.Part B: Maximum Sum Along With Subarray (Follow-up)) i/p:[-2, 1, -3, 4, -1, 2, 1, -5, 4] o/p: maxSum: 6,subarray =[4,-1,2,1].
25)You are given an array of non-negative integers where each element represents the height of a vertical line drawn at that index.Choose two different lines such that together with the x-axis they form a container.Find the maximum amount of water the container can store.height = [1, 8, 6, 2, 5, 4, 8, 3, 7] o/p: 49(Ask GPT if you dont understand)
26)You are given an array of integers that may contain duplicate values.Write a function to count how many unique (distinct) numbers are present in the array.A number is considered unique if it appears at least once, regardless of how many times it occurs. i/p: [1, 1, 2, 2, 3, 3, 4, 5, 6, 7, 8, 9, 10, 9] o/p: 10.

/////////////////////Strings///////////////////
1)Remove Duplicates from a String I/p:hello world baby o/p:helo wrd bay(preserve spaces).
2)Reverse Words in sentence I/p:hello world baby o/p:olleh dlrow ybab.
3)Given a string s, count how many palindromic substrings exist in it.i/p: "madam" o/p: 2
4)Given a string s, find the longest palindromic substring exist in it.i/p: "banana" o/p: anana
5)Given a string s, find the longest substring that contains no repeating characters and return the substring.
6)From the 2 given strings s1="abcdxyz", s2="xyzabcd" find the LongestCommon substring, o/p: "abcd"
